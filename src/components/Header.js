import { HeaderElement } from '../styles/HeaderStyle';
export const Header = () => {
    return (
        <HeaderElement>
            <span> Desafio Campgemini - Solução</span>
        </HeaderElement>
    );
}